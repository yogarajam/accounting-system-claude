# API Test Configuration

# Base URL
base.url=http://localhost:8080
api.base.path=/api

# Authentication
username=admin
password=admin123

# Timeouts (milliseconds)
connection.timeout=30000
read.timeout=30000

# Test Data
test.account.code.prefix=TEST
test.reference.prefix=TEST-REF

# Logging
log.requests=true
log.responses=true